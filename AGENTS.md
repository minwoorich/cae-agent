# CAE Agent 저장소용 Codex 지침

## 사용자 인터페이스

이 저장소에서 사용자의 자연어 요청을 기본 인터페이스로 취급한다. 사용자가
PowerShell 명령을 직접 요청하지 않았다면 필요한 `cae-agent` 명령을 Codex가
선택하고 실행한다. CAE 설치, Workbench, SpaceClaim, Mechanical, 형상,
재료, 경계조건 또는 해석 요청에는 `.agents/skills/cae-agent/SKILL.md`를
읽고 그 워크플로와 승인 경계를 따른다.

## 개발 규칙

- 새 코드의 주석과 docstring은 구현 의도, 제약조건과 오류 처리를 이해할 수
  있도록 자세한 한국어로 작성한다.
- 식별자, 공개 CLI 옵션과 일반 Python 관례는 영어를 유지한다.
- 원본 입력, 사용자 프로젝트와 작업공간 결과를 자동으로 삭제하거나 덮어쓰지 않는다.
- 외부 명령의 성공은 종료 코드와 구조화된 반환 결과를 함께 확인한다.
- Ansys와 Codex가 없는 환경에서도 기본 import, 진단과 단위 테스트가 동작해야 한다.

## CAE 실행 안전

- AI 생성 코드, `--clear`, `--overwrite`, 기존 모델 변경과 해석 실행은 대상과
  영향을 설명하고 사용자 승인을 받은 뒤 수행한다.
- 사용자가 이미 Codex와 대화 중이면 Codex가 스크립트를 직접 작성하고 기존
  실행기를 사용한다. `generate`와 `run-agent`는 내부 Codex 어댑터 자체를
  시험하거나 사용자가 명시적으로 선택한 경우에만 사용한다.
- 토큰, API 키, 라이선스 정보, 사용자 절대경로와 비공개 CAD를 로그, 프롬프트,
  문서 또는 GitHub Issue에 기록하지 않는다.

## 검증

- 관련 단위 테스트를 먼저 실행하고 최종적으로 전체 pytest를 실행한다.
- Skill 변경 시 skill-creator의 `quick_validate.py`와 저장소 Skill 회귀
  테스트를 실행한다.
- 실제 Ansys 실행 여부와 테스트 대역으로만 검증한 범위를 명확히 구분해 보고한다.
